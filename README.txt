Los siguientes directorios necesitan permiso de escritura:

-assets
-protected/runtime

hay un submodulo de git que se llama SluggableBehavior, para actualizarlo hay que hacer:

git submodule init
git submodule update




